export default {
  // PostCSS configuration object.
  plugins: {
    tailwindcss: {}, // TailwindCSS plugin: processes @tailwind directives and utilities.
    autoprefixer: {}, // Autoprefixer plugin: adds vendor prefixes for better browser support.
  },
};
